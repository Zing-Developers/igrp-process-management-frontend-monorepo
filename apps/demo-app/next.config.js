/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@igrp/platform-process-management-client-ts', '@igrp/platform-process-management-types'],
}

module.exports = nextConfig